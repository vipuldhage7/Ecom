import { ProductPagingData } from './product-paging-data';

describe('ProductPagingData', () => {
  it('should create an instance', () => {
    expect(new ProductPagingData()).toBeTruthy();
  });
});
